#include <stdio.h>

int main() {
    int m = 10;
    int n = 5;
    int result;

    // Equivalent of SUM function in MIPS
    result = m + n;

    // Equivalent of printing out the result in MIPS
    printf("%d\n", result);

    return 0;
}
